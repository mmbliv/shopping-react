
import { CircularProgress, Box, } from '@mui/material'

const Loading = () => {
    return (
        <Box sx={{ margin: '0 auto', width: 100 }}>
            <CircularProgress />
        </Box>
    )
}

export default Loading
